<img
  src="https://i.imgur.com/dpd20EG.png"
  style={{ width: "60px", float: "right" }}
/>
# Assignment 1

# Question 1:

```js
°F to °C
Given the function fToC(fDegree) , try and fix it so that it correctly converts Fahrenheit degree (°F) to Celsius degree (°C).
(°F − 32) × 5/9 = °C

function fToC (fDegree) {
    const cDegree = (fDegree - 32 * 5 / 9);
  return cDegree
}
console.log(fToC(32)) // expected result: 0
```

## Answer:

```js
function fToC(fDegree) {
  const cDegree = ((fDegree - 32) * 5) / 9;
  return cDegree;
}
```

# Question 2:

```js

Earth to Moon The distance between the Earth and the Moon is about 30 times
Earth's diameter. Given Earth's diameter is 12,742 km, calculate the distance in
kilometers.

function calculateDistance () {
  const diameter = 12742 const;
  distance = diameter;
  return distance;
}

console.log(calculateDistance()) //expected result: 382260

```

## Answer:

```js
function calculateDistance() {
  const diameter = 12742;
  const distance = diameter * 30;
  return distance;
}
console.log(calculateDistance()); // expected result: 382260
```

# Question 3:

```js

Given the function calculateBMI(height, weight) , try and fix it so that it
correctly calculate the BMI with the given height and weight.

function calculateBMI (height, weight) {
    const bmi = weight / height*height
  return "your BMI is " + bmi
  }
console.log(calculateBMI(1.6, 55)) // expected result: "your BMI is 21.48

```

## Answer:

```js
function calculateBMI(height, weight) {
  const bmi = weight / (height * height);
  return "your BMI is " + bmi;
}
console.log(calculateBMI(1.6, 55)); // expected result: "your BMI is 21.48"
```

# Question 4:

```js

Write a function to check if you are allowed to drink beer in New York.

function canDrinkBeer(age) {
    if(age > 21) {
      return "You are " + age + ", you can drinkbeer in New York"
  } else {
    return "You are " + age + ", you can not drink beerin New York"
    }
  }

  console.log(canDrinkBeer(21))

```

## Answer:

```js
function canDrinkBeer(age) {
  if (age >= 21) {
    return "You are " + age + ", you can drink beer in New York";
  } else {
    return "You are " + age + ", you can not drink beer in New York";
  }
}
console.log(canDrinkBeer(21));
```

<br />
<br />
<br />
